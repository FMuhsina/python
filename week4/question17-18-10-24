integers=[]
size=int(input("Enter number of integers in the list : "))
for i in range(0,size):
    print("enter integer ",i+1,":")
    integer=int(input())
    if integer>100:
        integers.append("over")
    else:
        integers.append(integer)
print("the list of inters (integer greater than 100 is printer as 'over')=")
print(integers)
